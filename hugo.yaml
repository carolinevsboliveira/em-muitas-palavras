# Hugo configuration file
title: Entre muitas palavras
defaultContentLanguage: pt
languageCode: pt

# import hextra as module
module:
  imports:
    - path: github.com/imfing/hextra

markup:
  # allow raw html
  goldmark:
    renderer:
      unsafe: true

  # enable hextra syntax highlight
  highlight:
    noClasses: false

menu:
  main:
    - name: Resenhas
      pageRef: /resenhas
      weight: 1
    - name: Sobre
      pageRef: /about
      weight: 2
    - name: Search
      weight: 3
      params:
        type: search
    - name: Theme
      weight: 4
      params:
        type: theme-toggle
params:
  description: "Um espaço entre livros, palavras e pensamentos."
  imageZoom:
    enable: true
  navbar:
    displayTitle: false
    displayLogo: true
    logo:
      path: images/logo.svg
      dark: images/logo-dark.svg
      link: /
      width: 176
      height: 32

  footer:
    enable: false

  theme:
    default: dark
    displayToggle: true

  page:
    width: normal

  editURL:
    enable: true
    base: "https://github.com/imfing/hextra-starter-template/edit/main/content"
